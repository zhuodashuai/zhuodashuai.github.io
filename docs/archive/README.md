# Archive

This directory keeps historical preservation records and dated QA evidence out
of the repository root. These files describe earlier builds and are not the
current release status.

Active automated tests live in `/tests/`, and the current human semantic test
matrix lives in `/tests/fixtures/semantic-test-cases.md`.
